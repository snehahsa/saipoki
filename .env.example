# Web app reads bot/.env first. Copy shared settings there, or override locally:

# BOT_TOKEN=          # must match bot/.env (same Telegram bot)
# WEBAPP_URL=
# GAME_SERVER_URL=
# ALLOWED_ORIGINS=
# START_BALANCE=5000
